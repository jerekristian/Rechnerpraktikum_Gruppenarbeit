<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.gruppenarbeit">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".DivisionRest_screen">
        </activity>
        <activity android:name=".kleinesEinsMalEins_screen"/>
        <activity android:name=".hilfe_screen"/>
        <activity android:name=".feedback_screen"/>
        <activity android:name=".rechen_screen"/>
        <activity android:name=".home_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".schwierigkeit_screen"/>
    </application>

</manifest>